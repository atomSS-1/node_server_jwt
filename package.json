{
  "name": "knance",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start:test": "export NODE_ENV=test || set NODE_ENV=test && nodemon ./bin/www",
    "start:dev": "export NODE_ENV=development || set NODE_ENV=development && nodemon ./bin/www",
    "start": "export NODE_ENV=production || set NODE_ENV=production && node ./bin/www"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv": "^10.0.0",
    "express": "~4.16.1",
    "http-errors": "~1.6.3",
    "jade": "~1.11.0",
    "jsonwebtoken": "^8.5.1",
    "morgan": "~1.9.1",
    "mysql2": "^2.2.5",
    "sequelize": "^6.6.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.7"
  }
}
